use std::cmp::*;
use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        a:i32,
        b:i32,
    }
    let mut ans = b / a;
    if b % a > 0 {
        ans += 1
    }
    println!("{}", ans);
}
