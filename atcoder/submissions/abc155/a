use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        a: i32,
        b: i32,
        c: i32,
    }
    if (a == b && a != c) || (b == c && a != c) || (a == c && b != c) {
        println!("Yes");
    } else {
        println!("No");
    }
}
