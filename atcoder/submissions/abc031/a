use std::cmp::*;
use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        a:i32,
        d:i32,
    }
    let b = (a + 1) * d;
    let e = a * (d + 1);
    println!("{}", max(b, e));
}
