use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        s:String,
        i:i32,
    }
    let sc = s.chars().collect::<Vec<char>>();
    println!("{}", sc[(i - 1) as usize]);
}
