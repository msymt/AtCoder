use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        a: i32,
        b: i32,
        c: i32,
    }
    let sum = a + b + c;
    if sum >= 22 {
        println!("bust");
    } else {
        println!("win");
    }
}
