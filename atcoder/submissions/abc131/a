use proconio::input;

fn main() {
    input!(s:String,);
    let vec:Vec<char>=s.chars().collect();
    for i in 1..4{
        if vec[i] == vec[i - 1]{
            println!("Bad");
            return;
        }
    }
    println!("Good");
}
