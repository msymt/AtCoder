use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        k: i32,
        x: i32,
    }
    if (500 * k) >= x {
        println!("Yes");
    } else {
        println!("No");
    }
}
