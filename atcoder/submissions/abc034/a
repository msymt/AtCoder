use std::cmp::*;
use proconio::input;
use proconio::marker::Chars;

fn main() {
    input!{
        x:i32,
        y:i32,
    }
    if y - x > 0 {
        println!("Better");
    } else {
        println!("Worse");
    }
}
